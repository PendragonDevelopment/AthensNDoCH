import React, { PropTypes } from 'react';
import ScheduleGridContainer from '../containers/ScheduleGridContainer.jsx';

export default Schedule = (props) => {
    return <div id="schedule">
        <h1>Schedule.</h1>
        <p>Blah blah blah schedule.</p>
        <ScheduleGridContainer></ScheduleGridContainer>
    </div>
}
